// ============================================================
// FRONTEND CONFIG — the only file you should need to edit for
// day-to-day changes. No secrets live here; this file is public.
// ============================================================
const APP_CONFIG = {
  // Point this at your backend. During local testing this is localhost;
  // after you deploy the backend (Render/Railway/etc.), change this to
  // that live URL, e.g. "https://api.tendrilandtwine.com"
  API_BASE_URL: "http://localhost:4000",

  WHATSAPP_SUPPORT_NUMBER: "918860357123", // country code + number, no + or spaces
  INSTAGRAM_HANDLE: "@tendrilandtwine",
  INSTAGRAM_URL: "https://instagram.com/tendrilandtwine",
};
